packages:
  - nginx
files:
  - path: "config.txt"
    content: 
      - |
        This is a config file
    metadata:
      - owner: "root"
      - group: "root"
      - mode: "test"
  - path: "nginx.conf"
    content: 
      - |
        This is a config file
    metadata:
      - owner: "root"
      - group: "root"
      - mode: "test"